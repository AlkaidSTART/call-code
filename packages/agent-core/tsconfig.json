{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",

    "declaration": true,          // 👉 生成类型定义（很重要）
    "composite": true             // 👉 支持被其他包引用（关键）
  },
  "include": ["src"]
}